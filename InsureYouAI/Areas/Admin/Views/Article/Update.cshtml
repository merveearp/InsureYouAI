@model Article

@{
    ViewData["Title"] = "Blog Güncelle";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="card shadow-sm border-0 rounded-3">
    <div class="card-body">

        <div class="d-flex justify-content-between align-items-center mb-3">
            <h5 class="mb-0 fw-bold text-primary">
                ✏️ Blog Yazısı Güncelle
            </h5>
        </div>

        <form method="post">

            <input type="hidden" asp-for="ArticleId" />

            <div class="mb-3">
                <label class="form-label fw-semibold">
                    Blog Başlığı
                </label>

                <input type="text"
                       asp-for="Title"
                       class="form-control"
                       required />
            </div>


            <div class="mb-3">
                <label class="form-label fw-semibold">
                    Kategori
                </label>

                <select asp-for="CategoryId"
                        asp-items="ViewBag.Categories"
                        class="form-control">
                </select>
            </div>


            <div class="mb-3">
                <label class="form-label fw-semibold">
                    Ana Kapak Görseli
                </label>

                <div class="input-group">
                    <span class="input-group-text bg-light">
                        <i class="bi bi-image"></i>
                    </span>

                    <input type="text"
                           asp-for="MainCoverImageUrl"
                           id="mainImageInput"
                           class="form-control" />
                </div>

                <div class="mt-3">
                    <img id="mainImagePreview"
                         src="@Model.MainCoverImageUrl"
                         class="shadow-sm border rounded"
                         style="max-width:250px;" />
                </div>
            </div>


            <div class="mb-3">
                <label class="form-label fw-semibold">
                    Blog Detay Görseli
                </label>

                <div class="input-group">
                    <span class="input-group-text bg-light">
                        <i class="bi bi-image"></i>
                    </span>

                    <input type="text"
                           asp-for="CoverImageUrl"
                           id="detailImageInput"
                           class="form-control" />
                </div>

                <div class="mt-3">
                    <img id="detailImagePreview"
                         src="@Model.CoverImageUrl"
                         class="shadow-sm border rounded"
                         style="max-width:250px;" />
                </div>
            </div>


            <div class="mb-3">
                <label class="form-label fw-semibold">
                    Blog İçeriği
                </label>

                <textarea asp-for="Content"
                          class="form-control"
                          rows="6"></textarea>
            </div>


            <div class="d-flex gap-2 mt-3">

                <a href="/Admin/Article/Index"
                   class="btn btn-outline-danger">
                    Vazgeç
                </a>

                <button class="btn btn-outline-success px-4">
                    Güncelle
                </button>

            </div>

        </form>

    </div>
</div>


<script>

    const mainInput = document.getElementById("mainImageInput");
    const mainPreview = document.getElementById("mainImagePreview");

    const detailInput = document.getElementById("detailImageInput");
    const detailPreview = document.getElementById("detailImagePreview");

    mainInput.addEventListener("input", function () {

        const url = this.value;

        if (url.length > 0) {
            mainPreview.src = url;
        }

    });


    detailInput.addEventListener("input", function () {

        const url = this.value;

        if (url.length > 0) {
            detailPreview.src = url;
        }

    });

</script>